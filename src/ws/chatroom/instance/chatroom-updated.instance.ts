import { ChatroomBaseInstance } from '../chatroom-base.instance'

// eslint-disable-next-line ts/consistent-type-imports
import { ChatroomUpdatedEvent } from '../dto/chatroom-updated.event'

/**
 * @category Instances
 */
export class ChatroomUpdatedInstance extends ChatroomBaseInstance<ChatroomUpdatedEvent> { }
